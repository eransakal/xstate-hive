{{#if commented}}/*{{/if}}
{{#if isKME}}
import { create{{pascalCase name}}MachineLogger } from '{{loggerPath}}logger';
import { XStateMachineLogSenderTypes } from '@kme/room-diagnostics-utils-diagnostics';

const logger = create{{pascalCase name}}MachineLogger(
  '{{loggerName}}',
  XStateMachineLogSenderTypes.{{loggerType}}
);
{{else}}
import { createUserPollsMachineLogger } from './logger';

const logger =  create{{pascalCase name}}MachineLogger(
    '{{loggerName}}'
  );
{{/if}}
{{#if commented}}*/{{/if}}