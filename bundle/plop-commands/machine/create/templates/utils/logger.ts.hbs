import { {{pascalCase name}}MachineId } from '../types';

export const create{{pascalCase name}}MachineLogger = (
  sender: string
) => ({
  log: ({ message, data }: { message: string, data?: Record<string, any>}) =>{
    if (data) {
      console.log(`[${ {{pascalCase name}}MachineId }:${sender}] ${message}`, data)
      return
    }
    console.log(`[${ {{pascalCase name}}MachineId }:${sender}] ${message}`)
  },
  warn: ({ message, data }: { message: string, data?: Record<string, any>}) =>{
    if (data) {
      console.warn(`[${ {{pascalCase name}}MachineId }:${sender}] ${message}`, data)
      return
    }
    console.warn(`[${ {{pascalCase name}}MachineId }:${sender}] ${message}`)
  },
  error: ({ message, data }: { message: string, data?: Record<string, any>}) =>{
    if (data) {
      console.error(`[${ {{pascalCase name}}MachineId }:${sender}] ${message}`, data)
      return
    }
    console.error(`[${ {{pascalCase name}}MachineId }:${sender}] ${message}`)
  }
})
