import {
  {{pascalCase machineName}}MachineEventsTypes,
  {{pascalCase machineName}}MachineStateConfig,
} from '{{relativePathToMachine}}types';

export const {{camelCase stateOffName}}State: {{pascalCase machineName}}MachineStateConfig = {
  states: {
    {{#if stateOffFinal}}type: 'final'{{/if}}
  },
};
