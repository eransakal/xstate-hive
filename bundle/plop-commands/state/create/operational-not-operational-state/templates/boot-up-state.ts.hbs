import { {{pascalCase machineName}}MachineStateConfig } from '{{relativePathToMachine}}../../types';
export const bootUpState: {{pascalCase machineName}}MachineStateConfig = {
  type: 'parallel',
  onDone: {      
      target: 'operational',
  },  
  always: {
    target: 'operational',
  },
  states: {    
    /*
    // Example of a parallel state that loads multiple resources in parallel.
    // If used, remove the always target above.
    loadXXX: {
      initial: 'loading',
      states: {        
        loading: {
          invoke: {
            src: 'get{{pascalCase machineName}}',
            onDone: {
              actions: ['set{{pascalCase machineName}}'],
              target: 'loaded',
            },
            onError: {
              target: 'error',
            },
          },          
        },
        loaded: {
          type: 'final',
        },
        error: {
        },
      },
    },
    */
  }  
};

