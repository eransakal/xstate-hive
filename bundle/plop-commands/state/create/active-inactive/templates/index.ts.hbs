import { {{pascalCase machineName}}MachineStateConfig } from '{{relativePathToMachine}}../../types';
import { bootUpState } from './boot-up-state';
import { operatingState } from './operating-state';


export const coreState: {{pascalCase machineName}}MachineStateConfig = {
  initial: 'bootUp',
  states: {
    bootUp: bootUpState,
    operating: operatingState,
  },
};
