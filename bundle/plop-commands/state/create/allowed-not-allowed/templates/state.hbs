import {
  {{pascalCase machineName}}MachineStateConfig,
} from '../../types';

export const {{lowerCase stateName}}State: {{pascalCase machineName}}MachineStateConfig = {
  initial: 'unknown',
  states: {
    unknown: {
      always: [
        {
          cond: (context) => true,
          target: 'allowed',
        },
        {
          target: 'notAllowed',
        },
      ],
    },
    allowed: {
      
    },
    notAllowed: { } ,
  },
};
