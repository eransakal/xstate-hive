import {
  {{pascalCase machineName}}MachineEventsTypes,
  {{pascalCase machineName}}MachineStateConfig,
} from '{{relativePathToMachine}}../../types';

export const operatingState: {{pascalCase machineName}}MachineStateConfig = {
  on: {},
  states: {
    idle: {},
  },
};
